@model WebApplication2.Models.Order
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="fa">
<head>
    <meta charset="UTF-8">
    <title>پرداخت موفق</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <style>
        body {
            background: #e6f7e6;
            font-family: 'Vazirmatn', sans-serif;
            direction: rtl;
        }

        .success-card {
            width: 420px;
            margin: 60px auto;
            padding: 40px;
            border-radius: 15px;
            background-color: #fff;
            box-shadow: 0 8px 20px rgba(0,0,0,0.08);
            border-top: 6px solid #16a34a;
            text-align: center;
        }

            .success-card h5 {
                color: #16a34a;
                font-weight: 700;
                margin-bottom: 20px;
            }

        .btn-outline-success {
            width: 100%;
            padding: 12px;
            border-radius: 10px;
            font-weight: 600;
        }
    </style>
</head>
<body>
    <div class="success-card">
        <h5>✅ پرداخت با موفقیت انجام شد</h5>
        <p>مبلغ پرداختی: <strong>@Model.Amount.ToString("N0") تومان</strong></p>
        <p>با تشکر از شما، تراکنش ثبت شد.</p>
        <a href="/Payment/Payment" class="btn btn-outline-success mt-3">بازگشت به صفحه اصلی</a>
    </div>
</body>
</html>
